---
phase: 02-full-api-coverage
plan: 06
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - agent-harness/cli_anything/attio/utils/attio_client.py
  - agent-harness/cli_anything/attio/webhooks.py
  - agent-harness/cli_anything/attio/workspace.py
  - agent-harness/cli_anything/attio/tests/test_webhooks.py
  - agent-harness/cli_anything/attio/tests/test_workspace.py
  - agent-harness/cli_anything/attio/tests/conftest.py
  - agent-harness/cli_anything/attio/attio_cli.py
autonomous: true
requirements:
  - HOOK-01
  - HOOK-02
  - HOOK-03
  - HOOK-04
  - HOOK-05
  - WORK-01
  - WORK-02
  - WORK-03

must_haves:
  truths:
    - "User can create, list, get, update, and delete webhook subscriptions"
    - "User can list workspace members and get a member by ID"
    - "User can identify current token/workspace via `attio workspace self`"
    - "All 11 resource groups are registered in CLI — full API coverage achieved"
    - "Full test suite passes across all command groups"
  artifacts:
    - path: "agent-harness/cli_anything/attio/webhooks.py"
      provides: "Webhooks command group"
      exports: ["webhooks_group"]
    - path: "agent-harness/cli_anything/attio/workspace.py"
      provides: "Workspace command group"
      exports: ["workspace_group"]
    - path: "agent-harness/cli_anything/attio/tests/test_webhooks.py"
      provides: "Webhooks tests"
      min_lines: 50
    - path: "agent-harness/cli_anything/attio/tests/test_workspace.py"
      provides: "Workspace tests"
      min_lines: 30
  key_links:
    - from: "agent-harness/cli_anything/attio/attio_cli.py"
      to: "all command modules"
      via: "cli.add_command for all 11+ resource groups"
      pattern: "add_command"
---

<objective>
Add Webhooks and Workspace Members CLI commands, then verify full API coverage.

Purpose: Final resource groups. Webhooks are full CRUD. Workspace members are read-only plus a `self` command. After this plan, every Attio API endpoint has a CLI command. Wave 2 because this plan runs the full test suite as final verification, which requires all other command modules to be registered.
Output: webhooks.py, workspace.py + client methods + tests + final full-suite verification
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add webhooks and workspace client methods + command files + tests</name>
  <read_first>
    - agent-harness/cli_anything/attio/records.py
    - agent-harness/cli_anything/attio/utils/attio_client.py
    - agent-harness/cli_anything/attio/tests/test_commands.py
    - agent-harness/cli_anything/attio/tests/conftest.py
  </read_first>
  <files>
    agent-harness/cli_anything/attio/utils/attio_client.py,
    agent-harness/cli_anything/attio/webhooks.py,
    agent-harness/cli_anything/attio/workspace.py,
    agent-harness/cli_anything/attio/tests/test_webhooks.py,
    agent-harness/cli_anything/attio/tests/test_workspace.py,
    agent-harness/cli_anything/attio/tests/conftest.py
  </files>
  <action>
**attio_client.py — Add webhooks methods:**

Attio Webhooks API:
- `POST /v2/webhooks` — create webhook. Body: `{"data": {"target_url": "https://...", "subscriptions": [{"event_type": "record.created", "filter": {"$and": [{"field": "object_id", "operator": "equals", "value": "..."}]}}]}}`.
- `GET /v2/webhooks/{webhook_id}` — get webhook
- `GET /v2/webhooks` — list all webhooks
- `PATCH /v2/webhooks/{webhook_id}` — update webhook (target_url, subscriptions, status)
- `DELETE /v2/webhooks/{webhook_id}` — delete webhook

```python
# ── Webhooks operations ───────────────────────────────────────────────
def create_webhook(self, target_url: str, subscriptions: list[dict[str, Any]]) -> dict[str, Any]:
    return self._request("POST", "/webhooks", json={"data": {"target_url": target_url, "subscriptions": subscriptions}})

def get_webhook(self, webhook_id: str) -> dict[str, Any]:
    return self._request("GET", f"/webhooks/{webhook_id}")

def list_webhooks(self) -> dict[str, Any]:
    return self._request("GET", "/webhooks")

def update_webhook(self, webhook_id: str, data: dict[str, Any]) -> dict[str, Any]:
    return self._request("PATCH", f"/webhooks/{webhook_id}", json={"data": data})

def delete_webhook(self, webhook_id: str) -> dict[str, Any]:
    return self._request("DELETE", f"/webhooks/{webhook_id}")
```

**attio_client.py — Add workspace methods:**

Attio Workspace API:
- `GET /v2/workspace-members` — list workspace members
- `GET /v2/workspace-members/{member_id}` — get member by ID
- `GET /v2/self` — already exists as `self_check()`, reuse it for WORK-03

```python
# ── Workspace operations ──────────────────────────────────────────────
def list_workspace_members(self) -> dict[str, Any]:
    return self._request("GET", "/workspace-members")

def get_workspace_member(self, member_id: str) -> dict[str, Any]:
    return self._request("GET", f"/workspace-members/{member_id}")
# self_check() already exists — reuse for workspace self command
```

**webhooks.py — Command group:**

`webhooks_group = click.Group("webhooks", help="Manage webhook subscriptions.")`:
- `create` — options: `--target-url` (required), `--subscriptions` (required, JSON string of subscription array), `--json`. Parse subscriptions JSON.
- `get` — argument: `webhook_id`, option: `--json`
- `list` — option: `--json`
- `update` — argument: `webhook_id`, option: `--data` (required, JSON), `--json`. Parses JSON data for flexible updates.
- `delete` — argument: `webhook_id`, options: `--yes`, `--json`

**workspace.py — Command group:**

`workspace_group = click.Group("workspace", help="Workspace information and members.")`:
- `members` — option: `--json`. Lists all members. Calls `client.list_workspace_members()`.
- `member` — argument: `member_id`, option: `--json`. Gets a single member. Calls `client.get_workspace_member()`.
- `self` — option: `--json`. Shows current workspace/token info. Calls `client.self_check()`.

**conftest.py — Add canned responses:**

```python
SAMPLE_WEBHOOK = {
    "id": {"webhook_id": "whk_abc123"},
    "target_url": "https://example.com/webhook",
    "status": "active",
    "subscriptions": [{"event_type": "record.created"}],
    "created_at": "2026-03-30T12:00:00.000Z",
}

SAMPLE_WORKSPACE_MEMBER = {
    "id": {"workspace_member_id": "wm_abc123"},
    "name": "Mitch Keller",
    "email_address": "mitch@leadgrow.ai",
    "role": "owner",
}
```

Add mock returns to `mock_client`. `list_workspace_members` returns `{"data": [SAMPLE_WORKSPACE_MEMBER]}`. `self_check` is already mocked.

**test_webhooks.py — Tests:**

Classes: `TestWebhooksCreate`, `TestWebhooksGet`, `TestWebhooksList`, `TestWebhooksUpdate`, `TestWebhooksDelete`.

- `TestWebhooksCreate.test_create_passes_target_url_and_subscriptions` — assert client called with correct args
- `TestWebhooksDelete.test_delete_with_yes` — standard delete pattern
- `TestWebhooksUpdate.test_update_passes_data` — assert JSON data passed through

**test_workspace.py — Tests:**

Classes: `TestWorkspaceMembers`, `TestWorkspaceMember`, `TestWorkspaceSelf`.

- `TestWorkspaceMembers.test_list_members` — asserts `client.list_workspace_members()` called
- `TestWorkspaceSelf.test_self_shows_workspace` — asserts `client.self_check()` called
  </action>
  <verify>
    <automated>cd agent-harness && python -m pytest cli_anything/attio/tests/test_webhooks.py cli_anything/attio/tests/test_workspace.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "def create_webhook" agent-harness/cli_anything/attio/utils/attio_client.py returns 1
    - grep -c "def list_workspace_members" agent-harness/cli_anything/attio/utils/attio_client.py returns 1
    - grep "webhooks_group" agent-harness/cli_anything/attio/webhooks.py returns matches
    - grep "workspace_group" agent-harness/cli_anything/attio/workspace.py returns matches
    - grep "self_check" agent-harness/cli_anything/attio/workspace.py returns match (reuses existing)
    - All tests pass (exit code 0)
  </acceptance_criteria>
  <done>Webhooks CRUD and workspace (members/self) commands work with mocked client</done>
</task>

<task type="auto">
  <name>Task 2: Register final groups + run full test suite</name>
  <read_first>
    - agent-harness/cli_anything/attio/attio_cli.py
    - agent-harness/cli_anything/attio/webhooks.py
    - agent-harness/cli_anything/attio/workspace.py
  </read_first>
  <files>agent-harness/cli_anything/attio/attio_cli.py</files>
  <action>
1. Add imports: `from .webhooks import webhooks_group` and `from .workspace import workspace_group`
2. Add: `cli.add_command(webhooks_group)` and `cli.add_command(workspace_group)`
3. Verify ALL resource groups are registered. The final attio_cli.py should have add_command calls for: people, companies, deals, users, workspaces, records_group, config_group, notes_group, tasks_group, comments_group, threads_group, lists_group, entries_group, objects_group, attributes_group, files_group, meetings_group, webhooks_group, workspace_group.
4. Run the FULL test suite: `python -m pytest cli_anything/attio/tests/ -v` to confirm every command group works together.
5. Verify CLI command count: `python -c "from cli_anything.attio.attio_cli import cli; print(len(cli.commands), 'commands:', sorted(c.name for c in cli.commands.values()))"` — should show 19 top-level commands.
  </action>
  <verify>
    <automated>cd agent-harness && python -m pytest cli_anything/attio/tests/ -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "add_command" agent-harness/cli_anything/attio/attio_cli.py returns at least 19
    - Python CLI import shows 19 registered commands (people, companies, deals, users, workspaces, records, config, completion, notes, tasks, comments, threads, lists, entries, objects, attributes, files, meetings, webhooks, workspace)
    - Full test suite passes with 0 failures
    - No import errors when loading CLI
  </acceptance_criteria>
  <done>All 11 Phase 2 resource groups registered in CLI. Full test suite passes. Complete API coverage achieved.</done>
</task>

</tasks>

<verification>
1. Full test suite: `cd agent-harness && python -m pytest cli_anything/attio/tests/ -v` — all pass
2. Command count: 19+ top-level commands registered
3. Every Phase 2 requirement has a corresponding CLI command
4. No import errors, no circular dependencies
</verification>

<success_criteria>
- 7 new AttioClient methods (5 webhooks + 2 workspace members)
- webhooks.py with 5 CRUD commands
- workspace.py with 3 read-only commands (members, member, self)
- Full test suite passes across ALL command groups
- 19+ commands registered in CLI entry point
- Phase 2 complete — every Attio API endpoint has a CLI command
</success_criteria>

<output>
After completion, create `.planning/phases/02-full-api-coverage/02-06-SUMMARY.md`
</output>
